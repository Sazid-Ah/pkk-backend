# Environment variables
.env
.env.local
.env.*.local

# SSH / private keys — never commit
*.pem
*.key

# Node.js
node_modules
# package-lock.json is intentionally NOT ignored — Vercel needs it to install the
# exact tree that was tested, and without it every deploy resolves versions afresh.

# Vercel
.vercel

# Build files
build

# Logs
logs

# Local uploads
uploads/

